# AceJob

Job hunt tracker built with React, Vite, Supabase Auth, Supabase Postgres, and Supabase Storage.

## Local setup

Install dependencies:

```sh
npm install
```

Create `.env.local`:

```sh
VITE_SUPABASE_URL=https://tqrhfnvrczfwqnjaehug.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your_supabase_publishable_key
VITE_SUPABASE_BUCKET=job-files
```

Run locally:

```sh
npm run dev
```

## Checks

```sh
npm run lint
npm run typecheck
npm run build
```
